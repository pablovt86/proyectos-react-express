export const types = {
    // informacion decriptiva desde el navegador. como esta el estado
    // va  amostrar la acciones en ese momento 
    // una va a ser la que loguea y la otra la que desloguea
    login : '[Auth] login',
    logout : '[Auth] logout'
}